apiVersion: v1
kind: Service
metadata:
  name: {{ include "reasoner.fullname" . }}
  labels:
    {{- include "reasoner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reasoner.selectorLabels" . | nindent 4 }}

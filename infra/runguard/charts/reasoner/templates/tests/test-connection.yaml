apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reasoner.fullname" . }}-test-connection"
  labels:
    {{- include "reasoner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reasoner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

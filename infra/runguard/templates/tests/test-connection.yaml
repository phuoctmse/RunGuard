apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runguard.fullname" . }}-test-connection"
  labels:
    {{- include "runguard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name }}-api-gateway:8080/healthz']
  restartPolicy: Never
